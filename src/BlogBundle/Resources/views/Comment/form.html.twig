{{ form_start(commentForm, { 'attr': {'class': 'col-md-4'}}) }}
{{ form_errors(commentForm) }}
<div class="form-group row req">
    {{ form_row(commentForm.content, {'attr': {'class': 'form-control comment-text', 'rows': '5'}}) }}
</div>
<div class="form-group row col-md-8 col-md-offset-2">
    {% if commentForm.add is defined %}
        {{ form_row(commentForm.add, {'attr': {'class': 'form-control btn btn-success'}}) }}
    {% elseif commentForm.edit %}
        {{ form_row(commentForm.edit, {'attr': {'class': 'form-control btn btn-success'}}) }}
    {% endif %}
</div>
{{ form_end(commentForm) }}